# 图文发布发版前复核（2026-09-12）

用户已有知乎、头条的 Studio 发布及公开全文、三图验收记录；小红书、微博、B站仍不能宣称自动发布全链路稳定。本轮用户要求“检查没有问题后提交推送并发布”，真实验收条件尚未满足，因此未提交、推送、改版本或触发正式发布。没有扩张下一平台。

## 用户功能验收

验收动作：在 Studio 选择已授权账号和原稿，查看执行计划随真实动作变化，由 Studio Agent 完成正文、三图、平台字段及最终提交，随后打开公开结果逐字核对正文、逐张核对图片及顺序。等待、失败和结果未知必须如实显示。

| 平台 | 当前证据与缺口 | 判断 |
|---|---|---|
| 小红书 | 作品已实际发布；自动公开图片映射核验未闭环，WebAffair 仍为结果未知 | 不能用人工看图替代自动闭环 |
| 知乎 | 已有 Studio 公开全文及三图核验记录 | 已有真实闭环；本轮未重复发布 |
| 微博 | 已有公开作品，但当时最终发送不是 Studio 执行；Studio 任务停在提交前 | 缺 Studio 实际提交与结果核验 |
| 头条 | 已有 Studio 公开全文及三图核验记录 | 已有真实闭环；本轮未重复发布 |
| B站 | 测试事务 G5 已自动提交匹配原稿及三图的请求，平台返回 `4126021`，无成功 ID/公开链接 | 账号类错误阻塞；不能宣称公开成功 |

B站执行计划验收与发布闭环分别判断：真实 UI 已显示账号阻塞提示，但最新主进程计划状态修复尚未在运行应用中加载验收；公开全文和三图也未验收。普通未知结果保护、历史提交事实和上传次数均保留，没有修改持久化状态解除保护。

本轮再次只读核对 B站测试事务 `248d5211-9d05-477c-a8db-50a4d75f0d84`：G5 为 `result-unknown`，提交步骤 `needs-reconcile`，公开核验仍待执行。三图累计各上传三次，不能通过清零计数继续重试。

真实界面证据：

- [Studio 账号阻塞提示](../../artifacts/article-bilibili-agent-20260912/same-task-g5-account-block-in-studio.jpeg)
- [平台账号资格页面](../../artifacts/article-bilibili-agent-20260912/same-task-g5-registered-member.jpeg)
- [详细现场与诊断记录](article-publishing-bilibili-confirmation-debug-2026-09-12.md)
- [逐平台计划及验收记录](../features/air-diary-platform-publishing-plan-2026-09-11.md)

## 本轮复核修复

1. B站首次规范确认前重新验证 `bodyStructureValid`。正文可见文字相同、原生编辑结构已损坏时也禁止发送；增加对应回归分支。
2. 修正三处源码格式问题。
3. 全库检查发现远程工具消息旧测试仍要求折叠组立即生成明细。组件当前按展开状态延迟渲染；更新测试核对动作/完成摘要及折叠状态不渲染大结果，不修改运行时行为。

## 工程准备度

- 修复后完整 `pnpm verify` 退出码 0：边界门禁、格式、lint、全库测试、类型检查和生产构建通过。379 个测试文件通过，2708 个用例通过、2 个跳过。
- `node scripts/bilibili-confirmation-smoke.cjs` 退出码 0，12 个隔离 Electron 场景全部通过，覆盖延迟规范页面、取消、正文变化及原生输入。它使用受控页面，不是 B站真实发布成功证据。
- `git diff --check` 通过。代码和证据保留在本地工作区，未提交推送。

## 后续交付条件

补齐小红书自动图片核验、微博 Studio 提交验收；B站需先处理账号侧限制，再通过有界恢复能力完成提交和公开核验。账号页面显示注册会员/LV0，但不能据此保证转正必然消除该错误。最新执行计划修复需在真实 Studio 验收。以上缺口解决前，工程测试通过不能替代产品稳定性结论。
