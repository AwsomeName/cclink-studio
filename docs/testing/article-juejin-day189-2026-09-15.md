# 第189天掘金真实发布验收（未闭环）

## 后续续聊入口修复

解锁后真实复验已证明：资源序列化修复生效，原会话可正常发送，Studio Agent 能执行账号列表、账号绑定、原稿导航与只读页面工具。普通续聊不能绑定已结束的发布 Attempt，应通过现有账号入口建立当前浏览器关联；旧 WebAffair 的未知结果保持不变。

随后真实发现普通账号浏览器的关键词保护把掘金顶部“发布”（设置入口）误判为最终提交。新增 `juejin-settings-entry.ts` 只读识别：原站点原稿 URL、唯一命中、可见且非 submit/非 form 的 xitu-btn“发布”、可见唯一 CodeMirror 和标题输入框，且发布设置与“确定并发布”均未显示时，识别为非最终入口。读取失败仍走原保护；回车、最终按钮、其他页面与不完整编辑器均不放行。原正式事务策略仍优先，不解除任何未知结果保护。

第一版固定选择器在 Agent 使用真实等效 CSS 路径时仍被拒绝；改为核验实际命中控件。63 项相关测试、node 类型检查、ESLint 通过后加载。Studio Agent 已实际成功打开原稿发布面板并继续读取摘要，未再次提交。截图：`artifacts/article-juejin-day189-20260915/settings-entry-agent-verified.jpeg`。等效路径识别由测试覆盖，本次成功实际使用 `button.xitu-btn:text-is("发布")`。

最终只读复验：Agent 核对原账号、原稿 URL 与标题，成功打开设置，读取摘要值为 21 字符、maxlength=100，当前可见摘要区域仅“编辑摘要”和“21/100”；未观察到明确阻止性提示。title/minlength 属性选择器探测超时，这不证明平台没有 JavaScript 校验，不能断言摘要是失败原因或是合规值。未点击面板内任何最终提交按钮，原发布结果仍留待用户人工确认。用户可在该原任务“打开 Agent”查看过程，再切掘金 Tab 查看已打开的发布设置；这只验收续聊与设置诊断能力，不代表增加一个已发布平台。

用户要求最终结果留待统一人工确认，继续优化 Studio 本身。对原发布会话发送只读诊断请求时，真实 UI 与 main 日志出现 `agent:sendMessage` 的 resources 校验错误“Agent 资源必须是标准 JSON 值”，Agent 尚未开始读取页面。

renderer `toSendResources` 无条件发送 `detail: undefined`，ref 也可能包含未赋值的可选属性；主进程标准 JSON 校验正确拒绝。修复发送投影，仅省略 undefined 可选字段，保留实际资源身份与 null 等值；不修改持久化资源、不放宽 IPC 校验，NaN 等非法数据仍拒绝。

payload 与 conversation-run-controller 共 26 项测试通过；新增用例直接通过真实 Agent IPC schema 校验浏览器资源续聊。web 类型检查及受影响 ESLint 通过，Vite 已热加载。真实续聊复验时桌面控制连续出现剪贴板读取超时和 noWindowsAvailable，尚未确认运行时续聊恢复。Studio PID 37842 仍运行；没有重启、提交代码或再次发布。

B站复查仍缺少失效旧路径对应的冻结原文证据，未放宽不同稿判断。本轮平台发布数量没有增加，CSDN、掘金继续留待用户人工确认。

## 用户功能进度

Studio 已复用唯一草稿，完成全文、三图、摘要、分类和删减后的标签核验，并由 Studio Agent 点击一次“确定并发布”；页面仍停在发布面板，尚无公开结果证据。用户已要求跳过 CSDN 核验，本轮没有再操作 CSDN 发布任务。

- 原文：`/Users/apple/Desktop/研发日记/AIR/2026-09-10/小红书软文.md`，三图仍为该日素材目录原图。
- 账号：`80a0a77d-3c2a-4a78-af5e-d37ea828d4cf`，真实用户「深芯智造」`4382008413004393`。
- 草稿：`https://juejin.cn/editor/drafts/7685253104890118196`。
- WebAffair：`b1c92d3a-acb1-4812-bba9-b5815e57cbc5`；Attempt：`894c672d-79e3-41f0-8659-3c875e4dd415`。

准备会话通过正常平台入口检查草稿箱为 0 篇，创建一份标题匹配的草稿，再回到草稿箱确认 1 篇及重开保存。正式任务绑定原稿，未建立重复稿。准备 Agent 仅凭 tag 页面可访问就推荐「智能眼镜」等标签，这不足以证明发布器提供同名候选，已在实测中暴露问题。

G1 首图上传在旧 `.bytemd-toolbar-tab:text-is("编辑")` 点击阶段超时；真实页面为分栏编辑器，没有该标签。G2 原稿恢复后仍为 0 图，因旧图片结果未知拒绝绑定 Runtime。根据可见完整原稿 0 图及源代码明确在 paste 之前超时的证据，通过 Studio 现有“网页里没有，重新上传”入口处理首图，不修改持久化文件。

G3 加载分栏兼容修复后，三图逐张上传成功，main 匹配平台 URL、加载、顺序和替代文字；正文由受限 browser_fill 回填冻结全文，bodyMatchesFrozen=true、saved。首图第二次尝试成功，其余两图第一次成功。随后摘要已填入，因空分类比较 undefined 与空字符串不一致而保存核验中断。

G4 加载空分类修复后，从同一原稿恢复。main 确认摘要已保存且匹配，不重写；分类「人工智能」及首个标签「人工智能」已确认。搜索第二个冻结标签「智能眼镜」时，真实下拉展开且输入值存在，但未提供匹配候选。适配器却硬编码 pendingValue 为空，误导 Agent 重试；已修正源码，并在后续 G5 重启时加载。

10:17 通过 Studio 浏览器运行卡暂停，WebAffair 为 needs-attention / checking-runtime、fill-fields，publication.status=not-started，三图 uploaded。没有点击最终“确定并发布”，没有公开全文或三图的成功证据。当时缺少原任务受限标签删减能力，后续已实现并实测，见下文。

## 工程准备度

- `juejin-editor-mode.ts`：编辑/预览区域已可见时无需点击旧标签；隐藏时只接受唯一可见切换入口，并保留派发权限复核。应用于图片上传及完整正文写入，已通过真实上传和正文回填。
- `juejin-publishing-adapter.ts`：空分类统一为空字符串，仍要求正文、标题、账号、摘要、分类和标签与服务端保存数据一致。已在 G4 真实恢复并填写分类验证。
- 标签 pendingValue 改为读取实际输入框，不再硬编码空值；本修复已随 G5 重启加载；本轮删减标签后无需再次搜索无效标签。
- 切换浏览器再回到未保存的新建任务表单会清空配置，本轮手动重新配置，尚未修复。
- 无提交、推送、发版；保留原有所有修改。

## 分别验收与证据

执行计划：真实经历上传失败、结果未知、原稿恢复、图片缺失确认、逐图成功、正文回读、摘要保存中断、字段恢复及暂停；最终步骤尚未完成。

发布闭环：G6 已由 Studio Agent 实际点击一次最终提交按钮（2026-09-15 12:32:52 +08:00），但未完成公开结果核验。WebAffair 已记录 publish:final 为 dispatched，页面仍停在原稿发布面板，publishedLinks 为空。真人可在 Studio 原任务查看字段与保存完成，再切原稿核对面板状态。不能将点击完成当作平台发布成功，也不能据此重复提交。

截图位于 `artifacts/article-juejin-day189-20260915/`：
- `g1-upload-toolbar-blocked.jpeg`
- `g2-recovered-no-images.jpeg`
- `g3-summary-save-blocked.jpeg`
- `g4-tag-no-option-paused.jpeg`

最后工程验证：12 项相关测试、node 类型检查、受影响 ESLint 均通过。测试通过不代表发布闭环完成。


## 受限标签删减及 G5 / G6 续接实测

用户授权继续后增加 Studio UI 的“移除标签”操作，仅适用于掘金、未提交、中断于 fill-fields 且所有既有副作用均已落定的原任务；任何 publish 副作用、未知结果、过期 attempt/generation/launch、并发过期配置均拒绝。只允许删除既有标签且至少保留一个，不允许增加或替换标签。

WebAffair 在同一串行状态队列中持久化删减事件，并重置字段、保存、发布和结果核验检查点，清除旧动作许可；正文、来源、账号、原稿、图片和历史副作用均保留。UI 通过类型化 IPC 调用，不提供 Agent 任意改配置接口，不直接编辑持久化文件。

实际通过 Studio UI 依次删除“智能眼镜”和“独立开发”，原任务保留“人工智能”，三图仍为 uploaded。G5 首次回读确认全文、三图、四字段和保存匹配，随后打开面板前命中 Runtime 连接代次变化，Agent 流空闲超时并被进度保护中断。通过原任务“从中断处继续”进入 G6，未重建任务或草稿。

G6 main 核验字段完成、保存完成，Studio Agent 打开发布设置，再核验字段并点击一次“确定并发布”。按钮工具完成不代表提交被平台接受：随后页面仍停原稿，面板未关闭，公开链接为空。真实截图中摘要计数“21/100”呈红色，但尚无明确校验提示，不能据此断言失败原因或解除未知结果保护。Agent 请求网络和控制台日志诊断被现有工具权限拒绝；缺少提交瞬间校验错误或发布请求响应的受限诊断证据。

执行计划验收仅部分通过：已实测中断、删减配置后恢复、字段完成、保存完成与提交派发；未验收发布成功及公开全文三图。Agent 曾尝试跳过合法检查点状态转换，main 正确拒绝，随后按 running → verifying → completed 推进；细步骤旧证据展示仍需在完整闭环中进一步检查。

新增界面证据：
- `artifacts/article-juejin-day189-20260915/g5-tags-reduced.jpeg`
- `artifacts/article-juejin-day189-20260915/g6-submit-panel-remains.jpeg`

工程验证：新标签删减纯函数 11 项及 WebAffair 状态测试 128 项通过；此前相关适配器和 service 测试通过。最初集成测试夹具遗漏 store.save 参数，修正后重跑上述 139 项全过。web/node 类型检查、受影响 ESLint、Prettier 通过。未提交、推送或发版。

12:36:29 WebAffair 将 execution/publication 转为 result-unknown，真实执行计划第 50 项显示“动作已完成 · 本次结果未知”，第 51–53 项公开图片未观察到执行，第 54 项结果未知；证据 `g6-plan-result-unknown.jpeg`。随后通过原任务“核验发布结果”入口进行只读续接，12:37:40 Runtime 启动前报告“文章管理页尚未找到标题为……的公开文章”；实际页面为草稿箱，唯一同名原稿仍在，未创建新稿或再次提交。证据 `g7-review-no-public-result.jpeg`。草稿仍在和未找到公开结果均不足以证明平台从未受理，保留未知结果保护。下一直接缺口是有界获取平台提交校验/响应，并以确证的拒绝或公开结果对账；不能靠再次授权或解除保护解决。
