# 头条微头条真实验收（2026-09-11 至 09-12）

## 用户功能进度

这篇文章已由 Studio Agent 单次提交，头条审核通过，公开作品可访问：
<https://www.toutiao.com/w/1876018407641100/>。

G20（9月12日00:27:59）由 Studio Agent 通过当前 Page 适配器回读公开全文与三张图，再完成检查点和 Attempt：`affair.status=completed`、`execution.status=published`、`publication.status=published`、`attempt.status=succeeded`。本篇“原稿恢复→单次提交→公开结果核验”的用户闭环已通过。

截图复核发现三项公开图片证据误写到编辑阶段，已修正后续运行的记录位置。00:35:20 通过 Studio“重新核验公开全文与图片”从当前公开页再次读回全文、三图地址/顺序/加载，由 WebAffair 更新三项公开图片细步骤。没有迁移旧数据、重新打开已完成 Run 或再次提交。45项细步骤现在可查看各自实际状态及证据。

- 原稿：`/Users/apple/Desktop/研发日记/2026-09-09/小红书软文.md`
- 标题：做AI眼镜第188天，它开始看懂我的世界。
- 登记账号：`1345b278-f2bf-4d40-ac5f-437d8a0b3c14`；真实 UID `3777529577766638`，现名“你好小眸”。
- 原稿 ID：`1876018407641100`。
- Affair：`9d635bd0-22cd-4ad2-93b1-930b894cffec`；Attempt：`5ebf83cf-35b6-4fff-a06f-46aefb6bb10a`。
- 用户已经授权此账号、本稿和三张原图发布，并确认“顺序正确”。不重复索要授权。

## 实际操作与执行者

1. Studio 登记账号 Agent 从真实微头条编辑器逐张上传三张本地图片，填写原文，存草稿。原上传控件无反馈的尝试没有算成功；实际面板分别出现累计 1、2、3 张后回读编辑器图集。
2. 正式 WebAffair 从管理页按原 draftId 找回原账号原稿，回读424字正文及三图。适配器只重读当前页面实际观察到的 `/mp/agw/draft/get_ugc_draft`，核对 `code=0`、原 gid、全文、逐图 URI、顺序和真实加载。
3. 平台未保留本地原文件名，正式任务不能把旧 Agent 自述当上传证据。用户确认顺序后，经 Studio 逐图对应按钮登记三项关系；没有重传。
4. G14 真实恢复后核验冻结正文、标题、三图和保存一致，正文写入与保存动作分别显示“无需执行”，对应读回显示“已核验”。没有虚构本代次的新写入或保存。
5. G15 Studio Agent 关闭实际发文助手遮罩，点击主进程签发的已勾选配乐标签一次；下一次适配器读取原生 checkbox=false。首发=false，作品声明逐项可读。配乐重载可能重新开启，因此此项位于最后保存后、提交前，动作与结果分列。
6. **9月11日23:52，Studio Agent 单次点击 `button.publish-content`。** main 提交前核对原 UID、draftId、标题、冻结全文、已保存状态、三张对应图片及平台选项。真实请求为 POST `/mp/agw/article/wtt`，HTTP200、code=0。首次观测器尚未识别最终结果，所以保留未知并停止，没有再发。
7. 平台管理页首先显示本稿三图“审核中”。G16 只读恢复（9月12日00:04）实际读到“已发布”，主进程以原 UID、标题和逐图地址/顺序/加载唯一对应作品。G17 主进程点击已核验作品标题的只读入口，取得真实公开 URL，打开后可见全文、三图和23:52发布时间。
8. G18 真实遇到作品子页遮住原绑定 Tab，Agent 正确停在“网页不可见”；G19 人工交还后错误套入草稿写入许可恢复。修正为已提交任务只恢复结果核验，并恢复原 Tab 可见；G20 原网页首次 inspect、verifying 后重读、completed（带实际 publicationUrl）、finish Attempt 全部通过。已完成检查点没有重新执行。
9. Codex 操作的是 Studio 的任务、恢复按钮和界面；这次最终平台发布按钮由 Studio Agent 通过能力调用。没有让 Codex 代点网站发布，也没有直接修改持久事务数据。

## 细步骤的真实展示

当前任务显示45项细步骤，涵盖账号/Profile、原 Tab、管理页找稿、账号/ID/标题/保存逐项核验、Runtime、正文区域、每张图片的检查/入口/动作/核验、图集顺序与加载、全文、标题、平台声明、保存动作/结果、配乐动作/结果、提交前检查、单次提交、每张公开图和公开结果。

每项展示执行者、进入/完成条件、实际状态、证据、等待原因和允许的下一步。WebAffair 是唯一状态所有者；写入完成不等于结果核验完成。原图对应等待、未变化正文/保存跳过、配乐动作与回读、提交未知、管理页已发布均已经由真实执行驱动过。

公开页适配器及终态收尾已在 G20 真实运行通过；三张公开图片的独立证据在00:35再次实际读取后展示。构建和测试不计入上述用户能力。

## 本轮直接阻塞修复

- 原 Tab 精确 Page 绑定改用 Electron `getOrCreateDevToolsTargetId()` 与 CDP `Target.getTargetInfo`；不再以 Playwright `_guid` 冒充 targetId。有已知 native target 时拒绝匹配其他同 URL 页面。
- 原任务 native View 在独立窗口可见时允许恢复；其他同账号 Tab 可见不能冒充原 Tab。
- 已保存且冻结全文/三图完全一致的头条恢复走只读分支；主进程的当代读回、跳过动作记录及无未核验写入缺一不可。
- 配乐 selector 按实际嵌套标签匹配，前后检查原生复选框；已关闭、改代、取消、已提交都拒绝反向切换。
- 管理页只接受同账号、唯一标题卡片及所有已映射原图，不能只凭标题或数量认领结果。公开地址来自实际作品入口，不由 Agent 猜测。
- 发布回执观察器收敛到单次已武装的真实端点，返回码还要结合管理页逐图证据。完善后的观察器只有针对性测试，不能用本稿已发生的旧请求声称新实现也已实跑；不为验收再次提交。
- 公开页数字 UID 不可从不透明用户链接猜测。账号归属来自同次主进程管理页核验，绑定实际公开 URL 与原稿 ID；正文和逐图仍须在当前公开页面重新读取。

## 工程准备度

- native Page/BrowserManager/原图确认65项、恢复正文与状态约束147项，均在各自修改后通过。
- 收尾改动的状态机、策略、适配器、管理页和 service 共194项通过；后补回执与公开 DOM 回调相关81项通过（包含重叠策略测试，不作总数相加）。
- 最后公开图片刷新补丁：4个文件180项通过，覆盖已完成任务不重开、过期代次/错误URL/错图/改代拒绝；node/web TypeScript、针对性 ESLint 及 diff 空白检查通过。真实终态与逐图刷新另按上述应用记录验收。
- 未提交、未发版；保留既有修改及安全保护。没有新增发布账本、claim/report MCP、内容/图片哈希或迁移。

## 真人复核与界面证据

1. 打开 Studio → “研发日记”工作空间 → 文章发布 → 同标题“头条微头条”任务。
2. 展开任一图片及正文、保存、配乐、发布结果步骤，分别核对动作和读回证据。
3. 点击“核验发布结果”，应只从原账号管理页核验已提交作品并读公开页，禁止重传、重写、重新发布。
4. 当前已完成任务显示“已发布”，三张公开图分别“已核验”。可点击“重新核验公开全文与图片”再次只读检查，然后返回计划展开对应图片查看新证据时间；此动作已实跑，不产生新发布。

- [最终执行计划：提交与三张公开图分别完成](../../artifacts/article-toutiao-agent-20260911/formal-plan-published.jpeg)
- [单张公开图片的条件、真实证据与下一步](../../artifacts/article-toutiao-agent-20260911/formal-public-image-evidence.jpeg)
- [最终 WebAffair 事实及唯一提交记录](../../artifacts/article-toutiao-agent-20260911/formal-published-evidence.json)
- [平台管理页已发布和三图](../../artifacts/article-toutiao-agent-20260911/formal-platform-published.jpeg)
- [公开作品正文与封面](../../artifacts/article-toutiao-agent-20260911/formal-public-article.jpeg)
- [三项原图对应确认](../../artifacts/article-toutiao-agent-20260911/formal-three-assets-confirmed.jpeg)
- [原稿保存核验](../../artifacts/article-toutiao-agent-20260911/formal-plan-saved.jpeg)
- [早期逐图卡点示例](../../artifacts/article-toutiao-agent-20260911/formal-plan-image-waiting.jpeg)

历史 G10 `formal-recovery-evidence.json` 是恢复阶段快照，不是当前发布结果。

## 剩余范围

本篇验证了已存在原稿的恢复发布，不等于任意新稿从空白编辑器创建、逐图上传、保存、提交都已在正式 WebAffair 一次跑完。三张原图最初由 Studio 登记账号 Agent 上传，正式任务以用户确认加现场逐图证据认领既有图。下一篇仍需验收正式新稿创建/上传链路；完善后的即时提交回执观察器也不能用本次旧请求冒充实跑。其他平台不因本轮头条通过而自动计入完成。
