# 第189天 B站新稿创建阻塞

## 2026-09-15 Studio 只读核查

当晚当前页面核查取得新证据：Studio Agent 从同 UID 头像菜单实际链接打开个人中心，但导航至 `account.bilibili.com` 后因超出浏览器任务网站范围而暂停，后续 Agent 读取与截图工具被拒绝。Codex 仅通过真实 Studio 界面独立只读复核、留存截图，没有绕过工具权限继续操作网站。页面 `https://account.bilibili.com/account/home?spm_id_from=333.1387.0.0` 显示“艾瑞_B”“注册会员”“LV0 进度条”“0 / 1”“转正成为正式会员”“距离转正会员 还差一点点!”，AX 中手机“已绑定”、实名“已认证”。头像菜单同时显示“答题转正直升Lv.1”和“0动态”。当前截图为 `artifacts/article-bilibili-day189-20260915/current-account-member-status.jpeg`、`current-account-conversion-prompt.jpeg`，个人中心 AX 证据为 `current-account-member-status.ax.txt`。没有开始答题、激活或账号变更。

下一步先由用户通过官方账号页面处理转正资格；这不是发布授权缺失，也不保证转正后 4126021 必然消失。Studio 原稿路径变更后的安全恢复能力缺口仍需处理。Agent 最终文字把“当前空动态且用户未删除”推断为历史提交全部未落库，证据不足，本记录不采纳该推断，不改变任何旧结果未知状态。执行计划验收本轮仅增加只读账号诊断证据，跨站任务实际显示暂停；第189天 B站发布闭环仍未启动，本轮没有新的提交、代码改动或发版。

用户随后明确确认没有手动删除过三条旧动态。再次核对旧主进程事实发现，必须同时报告另一个独立阻塞：测试事务 `248d5211-9d05-477c-a8db-50a4d75f0d84` G5 保留了 `requestMatch=matched`、HTTP 200、`platformCode=4126021`，没有动态 ID。9月12日实测记录和界面证据显示当时账号为注册会员/LV0，账号类错误提示已存在于 Studio 原任务中。此前仅强调原文搬家导致新稿准入阻塞，遗漏了该平台账号错误，不能据此认为只修复路径就能发布。该码不能解释为确定封禁，也不保证会员转正必然解决。当前账号资格另交 Studio Agent 只读核查，禁止代为答题、认证或账号变更。

用户要求先由 Studio 尝试解决，无需先让用户逐项检查。通过旧第188天事务的“打开 Agent”入口发起只读核查，使用现有 `web_accounts_list` 与 `web_account_open` 绑定指定账号，不绑定已结束 Attempt，不新建发布事务。

Studio Agent 已打开 `https://space.bilibili.com/3546384070347419/dynamic`，页面显示“艾瑞_B”、UID `3546384070347419`、“视角：我自己”。“全部”动态流显示“好像没有东西诶”，等待条目、滚动一次、刷新一次后仍为空。三种旧标题均未找到，没有可用公开 URL、正文或逐图证据。Codex 锁屏前实际界面与 Agent 结果相符，截图保存在 `artifacts/article-bilibili-day189-20260915/studio-account-empty-feed.jpeg`。

源码独立检查：旧任务只保存源路径/size/mtime，没有可独立验证的正文内容哈希或冻结正文。`startTask` 在恢复前要求源文件及图片元数据一致；`isIndependentBilibiliDraft` 也必须读取未变的旧原文比较正文，不能用新路径的同名文件替代证明。旧任务的 main 细步骤保留正文核验通过及字符数，但字符数不足以重建正文身份。旧 B站结果缺少已归属回执 URL，现有恢复分支不会自动从空主页产生成功回执。

结论：完成了真实只读核查，仍未消除第189天新稿阻塞。空列表不能证明旧提交从未受理或从未发布，不能直接解除保护；旧结果未知、资产与来源记录均未改。没有新的上传、提交或发版，没有改代码降低门禁。电脑随后锁定，已请求解锁。后续缺少的是可核验的旧原文/平台结果证据，或经过明确人工对账的有界处理入口；不能仅靠再次授权、恢复时间戳或编辑 JSON 解决。

用户授权同一第189天文章与三图继续逐平台验证。通过 Studio 正常表单选择原稿、B站已有账号 `7c1ef8bb-7770-4c53-ad02-e4943f745fea`、目标 UID `3546384070347419`，勾选已有授权对应的单次提交选项。

创建被主进程拒绝，未建立第189天 B站事务、未上传、未提交。提示：该账号已有未结束任务；仅允许旧发布已中断且原文未变时创建标题、文件和正文均不同的新稿，未知原稿仍只能核验。

源码 `bilibili-independent-draft.ts` 要求旧源文件存在、size/mtime 与记录一致，且与新稿正文不同。三个旧未知任务的 `/Users/apple/Desktop/研发日记/2026-09-09/` 原稿路径均不存在；文件现位于 `AIR/2026-09-09/`。其中第188天原稿修改时间已变化，不能仅凭文件名/大小认定它与旧发布时原文相同。没有修改旧持久化状态、恢复时间戳、复制旧稿到原路径或放宽保护。

执行计划和发布闭环均未启动。后续需解决旧未知任务的原稿证据或真实平台结果核验；本轮转向独立的 CSDN 验证。不得将这一阻塞归因于B站登录或网络故障。未提交代码、推送或发版。
