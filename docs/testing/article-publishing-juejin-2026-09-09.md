# 掘金三图文章调试（2026-09-09）

状态：指定三图稿已实际发布，WebAffair 已核验完成；执行计划有两项历史观测残余，不能宣称全部细步骤验收完成。

## 真实任务

- 账号：深芯智造，平台用户 ID `4382008413004393`，Studio 账号 `80a0a77d-3c2a-4a78-af5e-d37ea828d4cf`。
- 原文：`/Users/apple/Desktop/chat-cc/cclink-promotion/03-linux.do/2026-08-27-App写完之后-初稿.md`，引用原目录三张 PNG。
- 标题：App 写完可以 close，上线和宣发却要一直做。
- 草稿：`https://juejin.cn/editor/drafts/7683025447916847150`。
- WebAffair：`17409bd7-4ee4-4265-853d-52ac84d9f30a`。
- 同一 Attempt：`8f70d4e3-b7c0-4dc1-a846-8be59f20437c`。

## 已观察到的事实

1. 同步 Profile 已登录；个人文章页有两篇旧文章，不含本稿，草稿箱原为空。
2. 调试准备通过平台真实标题输入建立草稿，并选择代码人生分类、程序员标签和文章摘要。此准备发生在 Studio 发布任务建立前，不补记为 Agent 自动新建/自动填字段成功。
3. Studio 新增掘金平台选项，沿用文章发布 Tab；当前从已有数字草稿 ID 启动，复用 WebAffair、长 Agent Run、Runtime handshake 和恢复保护。
4. 首次草稿列表尚未加载即被读取，启动安全停止。改为等待实际数字草稿链接；恢复同稿通过，完成步骤保留。
5. 首次图片派发错误定位到文档导入 input，30 秒无正文图片结果，停为结果未知。随后外层 CodeMirror 粘贴没有触发其输入框处理器，同样停止。两次均核对可见原稿和服务端正文为空，再通过现有图片缺失确认入口对账；这属于调试辅助确认，不是全自动上传成功。
6. 修正为向当前原稿的 CodeMirror textarea 发送带真实文件的粘贴事件，使用平台自己的上传处理器。每次文件派发仍经过原有副作用与取消闸门，结果必须是同账号同稿中新增的唯一已加载平台图片。

## 验收动作

在 Studio 文章发布入口选择掘金账号、上述原文及已有草稿标识，启动后查看管理页定位、账号/ID/标题/保存复核、三张图片各自的动作与结果、正文及图片位置、平台字段、自动保存、一次最终提交及公开结果。
“打开发布设置”与“确定并发布”分别处理。保存状态必须来自掘金草稿读取接口与当前编辑器完整 Markdown 比对，不能由页面提示或 Agent 自述代替。

## 当前边界

自动创建草稿尚未交付；分类、标签及摘要的自动修改分支尚未实测，本稿准备时已填写。封面、专栏、话题、声明及完整中断矩阵不属于已验收能力。本次真实结果与证据见下文，工程测试不代替业务闭环。

## 后续真实恢复结果

- 图片粘贴输入框修复后，三张图均实际进入掘金私有图片域名。第二张在域名接入补齐后通过现有“存在”确认入口，由 main 唯一对应并回读图片；不是再次上传。
- 私有图片签名在重新开稿以及平台保存时轮换，包括 `policy`、`rk3s` 和 `x-orig-*`。真实比对确认图片路径一致；只在 Markdown 图片语法中规范化这些已观察参数，普通链接和图片路径变化仍拒绝。
- 第一张已由 main 记录地址后因签名差异停住；修复后 Runtime handshake 自动复核同一草稿中的地址与加载状态，将其恢复为已上传，未要求再次上传。
- 第三张的上传、唯一图片回读与服务端保存本次自动完成；所有三张图均已有平台地址，随后全文、链接、三图顺序/位置/替代文字/加载均通过，平台字段已完成同值核验；随后完成一次发布和公开结果核验。
- 本轮未提交或发版。工作期间 HEAD 被其他操作更新到 `204a6cad`；继续保留当时工作区，不回退该提交。

## 最终发布结果

- 公开文章：[App 写完可以 close，上线和宣发却要一直做](https://juejin.cn/post/7683086663757004850)。未登录 HTTP 请求返回 200 并含完整原文。
- 原草稿读取结果将 `7683025447916847150` 映射到公开文章 `7683086663757004850`；当前账号和公开作者均为 `4382008413004393`。
- 全文规范化后 1610 字符一致，官网链接真实目标一致。三张图全部加载，顺序、替代文字及正文位置（23 / 701 / 1114）一致。公开页图片域名变化通过原平台图片路径对应，不引入哈希。
- 仅一次最终 publish 派发。首次公开页探针未适配真实正文容器而停止；修正后重启，同一 Attempt 的第 11 代只读核验，于 `2026-09-08T16:36:05.132Z` 得到 WebAffair completed、execution published、publication published，八个业务检查点完成，Agent 随后正常结束。没有重复发布。
- 只改变核验函数的预期 HTML，未改公开页：错误链接、少图、重复图、错误图片路径、正文改变五项负例均拒绝。

## 执行计划单独验收

真实 Electron 显示 54 个小步骤，包括每张图片独立动作/核验、字段同值跳过、保存和发布分离、公开页逐图核验。上传失败/未知时曾显示具体图片卡点，恢复后继续剩余图片和正文，最终显示已发布。
仍有两项历史观测残余：第二张图的“定位上传控件”没有逐图记录，界面保留“尚未观察到执行”；正文定位保留旧代次的等待复核提示，虽然公开页已通过。不能为了全绿补造记录，也不能把本次称作执行计划全部验收完成。这不改变一次真实提交及公开结果已核验的事实。

证据目录：`artifacts/article-juejin-20260909/`。
- `plan-waiting.png`：真实中断现场。
- `plan-publication-verified.png`：真实细步骤和公开结果核验。
- `published-full.png`：公开文章及三张图片。
- `public-readback.json`、`public-negative-readback.json`：真实正负核验结果。
- `final-state.json`、`final-plan.json`：最终事务状态及界面逐步结果。

人工复核：在 Studio 打开该掘金任务，滚动至执行计划，展开第 50—54 项查看一次派发与逐图/公开结果；打开上述公开链接检查账号、全文及三图。前述两项历史残余也应可见。

工程门禁：相关 17 文件 273 项回归通过；另新增原稿→公开文章映射保护测试后，掘金 adapter 4 项通过。最终 TypeScript 检查及受影响 ESLint 通过。本轮未提交、未发版。
