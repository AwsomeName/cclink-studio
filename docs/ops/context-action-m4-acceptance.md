# M4 领域上下文操作真人验收

> 状态：待验收。日期：2026-07-22。分支：`codex/context-action-m4`。

## 原则

- 一次只确认一项；失败立即停止，记录现象后修复并重跑门禁。
- 不输入、复制或粘贴真实凭证到验收记录。
- 运营和生产只验证准备、配置、状态与诊断，不执行远端发布、发送、下单或支付。

## 自动化基线

- [x] `pnpm verify`：154 files / 932 tests。
- [x] `pnpm smoke:standalone`：local 9/9、UI 6/6、workflow 8/8、restore 4/4。
- [x] workflow 验证运营、生产、设置的鼠标右键与 Shift+F10。
- [x] 单元测试验证危险领域命令缺失、凭证复制边界和可选能力降级。

## H1 数据源

- [ ] 在已配置的数据源、集合、保存查询或查询结果记录上右键，菜单目标与所点对象一致。
- [ ] “复制标识/记录 JSON”不包含连接密码、Token 或其他凭证；“挂载到 Agent”只挂载，不自动发送。

## H2 运营与生产

- [ ] 运营平台菜单只有准备会话、打开配置和复制状态；没有发布或发送命令。
- [ ] 生产区域菜单只有扫描、检查、本地报告和复制状态；没有下单、支付或远端提交命令。

## H3 Android 降级

- [ ] 未连接设备或缺少 adb 时，Android 菜单显示明确不可用原因，其他工作台区域仍可正常使用。
- [ ] 若已有设备，连接/断开动作只作用于当前 Android Tab。

## H4 设置安全边界

- [ ] 普通设置行右键可复制 setting key；已修改项可恢复默认。
- [ ] API Key、GitHub Token 等凭证输入行没有复制凭证的上下文命令。

## H5 跨模块隔离

- [ ] 在一个领域打开菜单后切换项目或 Tab，旧菜单关闭，动作不会落到新项目。
- [ ] 任一可选领域不可用时，文件、Browser、Terminal 和 Agent 的上下文菜单仍正常。

## 关闭条件

- H1-H5 全部通过并写回结果。
- 最新提交在全新 detached worktree 完成锁定安装、`pnpm verify` 与 `pnpm smoke:standalone`。
- 分支已推送，工作树干净；满足以上条件后才能把 M4 标记为完成。
